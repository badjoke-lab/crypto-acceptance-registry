# Export Scripts

This directory is for legacy data extraction from the old CPM dataset.

Initial targets:
- places
- payment_accepts
- socials
- verifications

Expected output:
- normalized candidate dataset for classification
- one file or stream per source dataset if needed
- stable field names for downstream classification
